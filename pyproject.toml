[project]
name = "imgbuilder"
version = "1.0"
description = ""
authors = [{ name = "Emilia", email = "voxelost@proton.me" }]
dependencies = ["jinja2>=3.1.2", "PyYAML>=6.0.1", "passlib>=1.7.4"]
requires-python = ">=3.11"
readme = "README.md"
license = { text = "MIT" }

[tool.pdm.dev-dependencies]
dev = ["jedi-language-server>=0.41.1", "mypy>=1.6.0"]
